{
  "name": "moonstone",
  "private": true,
  "description": "Personal ATProto PDS — optimised for NixOS/Caddy deployment",
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "type-check": "pnpm -r run type-check",
    "clean": "pnpm -r exec -- rm -rf dist"
  }
}
